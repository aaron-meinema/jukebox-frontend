{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "firefox",
        "request": "launch",
        "name": "Launch FireFox against localhost",
        "url": "http://localhost:4200",
        "webRoot": "${workspaceFolder}"
      }
    ]
  }